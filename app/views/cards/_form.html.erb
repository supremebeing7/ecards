<%= form_for @card, :url => "/cards", :html => { :multipart => true } do |form| %>

  <%= form.label "Name" %>
  <%= form.text_field :name %>


  <%= form.file_field :card_image %>

    <%= form.submit 'Add Image', class: 'btn btn-danger' %>

<% end %>
